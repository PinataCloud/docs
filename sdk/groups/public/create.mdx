---
title: "groups.public.create"
description: "Pinata SDK groups.public.create makes a new public file group on Pinata so you can organize public IPFS uploads. Requires org:groups:write."
---

Create a public group

## Usage

```typescript
import { PinataSDK } from "pinata";

const pinata = new PinataSDK({
  pinataJwt: process.env.PINATA_JWT!,
  pinataGateway: "example-gateway.mypinata.cloud",
});

const group = await pinata.groups.public.create({
	name: "My New Group",
});
```

## Returns

```typescript
type GroupResponseItem = {
    id: string;
    name: string;
    created_at: string;
};
```

## Parameters

### name

- Type: `string`

Requires a name for the group to be created

```typescript {2}
const group = await pinata.groups.public.create({
	name: "My New Group",
});
```
