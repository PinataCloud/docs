---
title: Signatures
description: "TypeScript types for the Pinata Signatures SDK, including SignatureOptions and the response shapes for adding, fetching, and deleting CID signatures."
---

### SignatureOptions
```typescript
export type SignatureOptions = {
	cid: string;
	signature: string;
	address: string;
};
```

### SignatureResponse
```typescript
export type SignatureResponse = {
	cid: string;
	signature: string;
};
```
