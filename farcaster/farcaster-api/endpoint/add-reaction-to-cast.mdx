---
openapi: post /reactions
---

To add a reaction to a cast provide the `castId` (object with the `hash` and `fid` of the user who posted it), the `signerId` of the user reacting, and the type of `reaction`. You can currently choose from `like` and `recast`

